<header>
  <nav>
    <a href={`/`}> トリコじかけの明け暮れ</a>
    <a href={`/rss.xml`} target="_blank"> rss</a>
    <a style="display: none;" href={`/sitemap.xml`} target="_blank"> sitemap</a>
  </nav>
</header>

<style scoped>
  header {
    position: sticky;
    top: 0;
    min-width: 0;
    z-index: 100;
    background: #e5edc8;
  }
  nav {
    display: flex;
    flex: 1;
    justify-content: flex-start;
    align-items: center;
    margin: auto;
    max-width: var(--container-width);
    text-transform: uppercase;
  }

  a {
    margin-left: 20px;
    padding: 10px 5px;
    display: block;
    position: relative;
    color: inherit;
    text-decoration: none;

    white-space: nowrap;
  }
  a:first-child {
    text-overflow: ellipsis;
  }
  a:last-child {
    margin-right: 20px;
  }

  a:not(.selected) {
    opacity: 0.7;
  }

  a::before {
    content: "";
    position: absolute;
    transition: transform 0.3s ease;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: #aaa;
    transform: scaleX(0);
  }

  a:hover::before,
  .selected::before {
    transform: scaleX(1);
  }

  .selected::before {
    background: #fd6378;
  }

  .selected::after {
    content: "";
    position: absolute;
    margin: auto;
    width: 100%;
    height: 4px;
    bottom: 0px;
    left: 0;
    background: radial-gradient(
      ellipse farthest-corner at 50% 100%,
      #fd6378 0,
      #fd6378 25%,
      hsla(0, 0%, 100%, 0) 75%
    );
    transform: scaleX(0);
    transition: transform 0.2s ease-in-out;
  }
  .selected:hover::after {
    transform: scaleX(1);
  }
</style>
